/*
* generated by Xtext
*/
package net.certware.argument.gsn.ui.labeling;

import net.certware.argument.arm.Claim;
import net.certware.argument.arm.ModelElement;
import net.certware.argument.gsn.Evidence;
import net.certware.argument.gsn.Goal;
import net.certware.argument.gsn.Solution;
import net.certware.argument.gsn.Strategy;

import org.eclipse.emf.edit.ui.provider.AdapterFactoryLabelProvider;
import org.eclipse.xtext.ui.label.DefaultEObjectLabelProvider;

import com.google.inject.Inject;

/**
 * Provides labels for a EObjects.
 * 
 * see http://www.eclipse.org/Xtext/documentation/latest/xtext.html#labelProvider
 */
public class GsnDslLabelProvider extends DefaultEObjectLabelProvider {

	@Inject
	public GsnDslLabelProvider(AdapterFactoryLabelProvider delegate) {
		super(delegate);
	}

	String text(Goal g) {
		return String.format("%s %s","Goal", ((Claim)g).getIdentifier());
	}

	String text(Strategy s) {
		return String.format("%s %s","Strategy", ((ModelElement)s).getIdentifier());
	}

	String text(Solution s) {
		return String.format("%s %s","Solution", ((ModelElement)s).getIdentifier());
	}
	
	String text(Evidence e) {
		return String.format("%s %s","Evidence", ((ModelElement)e).getIdentifier());
	}
	
	String image(Goal g) {
		// icons/ path already assumed 
		return "obj16/oval.png";
	}
	
	String image(Strategy s) {
		return "obj16/rectangle.png";
	}
	
	String image(Solution s) {
		return "obj16/circle.png";
	}
	
	String image(Evidence e) {
		return "obj16/circle.png";
	}
	
}
